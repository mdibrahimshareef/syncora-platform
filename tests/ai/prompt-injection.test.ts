import { describe, it, expect } from 'vitest'

describe('AI Prompt Injection Tests', () => {
  it('ignores "ignore previous instructions" embedded in task descriptions', () => {
    expect(true).toBe(true)
  })
})
